$(function() {
	/*
	$('article ul li a').click(function(){
		
		//console.log($(this).find('img').attr('data'));
		//console.log($(this).closest('article').find('img').first().attr('src'));

		var src=$(this).find('img').attr('rel');
		var target=$(this).closest('article').find('img').first();

		$(target).attr('src',src);

		
		return false;

	});*/




});